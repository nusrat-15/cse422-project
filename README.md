# cse422-project
This project is a machine learning notebook that analyzes a dataset and applies classification algorithms to predict outcomes based on input features. It includes data preprocessing, exploratory analysis, model training, and performance evaluation using standard metrics such as accuracy, precision, recall, and confusion matrix.
